const name = "World";

function greet(who) {
    print("Hello, " + who + "!")
}

function makeAdder(offset) {
    function add (x, y) {
        x + y + offset
    }

    add
}

const adder = makeAdder(10);
print(adder(5, 5))

nat("save")

greet(name)